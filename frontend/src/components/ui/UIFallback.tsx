import React from "react";

/** Fallback hook remains for API-error branches; the red "API DATA" label is not rendered. */
export function UIFallbackIndicator(_props: { style?: unknown }) {
  return null;
}
