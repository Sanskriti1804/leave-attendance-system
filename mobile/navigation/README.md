# Navigation

Routes are managed by Expo Router under `app/`.

When navigation was enabled at generation time, primary destinations live in `app/(tabs)/`.

```ts
// TODO: Add additional navigation routes here.
```
